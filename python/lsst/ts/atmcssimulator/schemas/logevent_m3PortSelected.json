{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "type": "object",
    "properties": {
        "id": {
            "const": "evt_m3PortSelected"
        },
        "selected": {
            "type": "number",
            "description": "The port that is to receive light from the system, or if stage is in motion"
        }
    },
    "required": [
        "id",
        "selected"
    ],
    "additionalProperties": false
}
